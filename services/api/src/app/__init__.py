"""Backend application package for the phrase-validation service.

Modules are added incrementally per the phrase-validation delivery plan:
`modules/similarity` and `modules/phrases` (domain/application/adapters/api layers) plus
`platform/` for cross-cutting concerns. See `openspec/changes/phrase-validation/design.md`.
"""
