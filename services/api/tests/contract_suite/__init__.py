"""Shared repository contract suite: one assertion set, run once per
registered `PhraseRepository`/`UnitOfWork` adapter (design.md: "the fast
fake cannot drift from the real one"). See `repository_contract.py`.
"""
